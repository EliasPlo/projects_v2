/* General Styles */
body {
    margin: 0;
    font-family: 'Arial', sans-serif;
    background-color: #f4f4f4;
    color: #333;
  }
  
  a {
    text-decoration: none;
  }
  
  /* Navigation Bar */
  ul {
    display: flex;
    justify-content: center;
    list-style: none;
    background-color: #222; /* Black background for the navbar */
    padding: 10px 0;
    margin: 0;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
  }
  
  li {
    margin: 0 15px;
  }
  
  li a {
    color: #f4f4f4; /* Light text for links */
    padding: 8px 15px;
    border-radius: 5px;
    transition: background-color 0.3s, color 0.3s;
  }
  
  li a:hover {
    background-color: #28a745; /* Green on hover */
    color: white;
  }
  
  li a.active {
    background-color: #dc3545; /* Red for active link */
    color: white;
    font-weight: bold;
  }
  
  /* Main Content */
  div {
    text-align: center;
    margin: 20px auto;
    max-width: 800px;
    padding: 20px;
    background: #fff; /* White card-like layout */
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
  }
  
  /* Buttons (optional if needed for components) */
  button {
    background-color: #28a745; /* Green */
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s, transform 0.2s;
  }
  
  button:hover {
    background-color: #218838; /* Darker green on hover */
    transform: scale(1.05);
  }
  
  button:active {
    background-color: #1e7e34; /* Even darker green on click */
  }
  
  /* Responsive Design */
  @media (max-width: 768px) {
    ul {
      flex-direction: column;
      padding: 15px 0;
    }
  
    li {
      margin: 10px 0;
    }
  
    div {
      margin: 10px;
      padding: 15px;
    }
  }
  